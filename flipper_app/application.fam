App(
    appid="vgm_usb_keyboard",
    name="VGM USB Keyboard",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="vgm_usb_keyboard_app",
    requires=["gui", "storage", "expansion"],
    stack_size=8 * 1024,
    fap_category="Tools",
    fap_version="1.0",
    fap_description="Use a USB keyboard on the VGM to write a text file",
)
